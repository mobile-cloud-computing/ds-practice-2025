syntax = "proto3";

package fraud_detection;

service FraudDetectionService {
    rpc InitializeFraudOrder (FraudRequest) returns (FraudResponse);
}

message FraudRequest {
    string card_number = 1;
    string order_amount = 2;
}

message FraudResponse {
    bool is_fraud = 1;
}
