// suggestion.proto

syntax = "proto3";

package suggestion;

service SuggestionService {
    rpc GetSuggestions (SuggestionRequest) returns (SuggestionsResponse);
}

message SuggestionRequest {
    string comment = 1;
}

message SuggestionsResponse {
    repeated string suggestions = 1;
}